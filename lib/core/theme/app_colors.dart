import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand
  static const Color primary = Color(0xFF24292F);
  static const Color accent = Color(0xFF0969DA);
  static const Color accentLight = Color(0xFFDDF4FF);

  // Backgrounds
  static const Color background = Color(0xFFFFFFFF);
  static const Color surface = Color(0xFFF6F8FA);
  static const Color surfaceElevated = Color(0xFFFFFFFF);

  // Text
  static const Color text = Color(0xFF1F2328);
  static const Color textSecondary = Color(0xFF656D76);
  static const Color textMuted = Color(0xFF9198A1);
  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // Borders
  static const Color border = Color(0xFFD0D7DE);
  static const Color borderMuted = Color(0xFFD8DEE4);

  // Status
  static const Color error = Color(0xFFCF222E);
  static const Color warning = Color(0xFF9A6700);
  static const Color success = Color(0xFF1A7F37);

  // Stars / forks
  static const Color star = Color(0xFFE3B341);
  static const Color fork = Color(0xFF656D76);

  // Splash gradient
  static const Color splashStart = Color(0xFF0D1117);
  static const Color splashEnd = Color(0xFF161B22);

  // Language colors (fallback)
  static const Color languageDefault = Color(0xFF8B949E);
}
